import { Intro, Meta, Props, Story } from '$storybook/docs'
import * as Stories from './PlainLayout.stories.svelte'

<Meta of={Stories} />

<Intro name="PlainLayout" description="A surface for displaying your app content." />

<Story of={Stories.Basic} />

## Properties

<Props />

## Transparent

On macOS, it supports transparent mode. In this mode, the background behind the component becomes completely transparent. Use this mode with the right background. See [WindowIsTranslucent](https://wails.io/docs/reference/options#windowistranslucent-1) in Wails.

<Story of={Stories.Transparent} />

## Inset

Wails supports customizing of the [title bar](https://wails.io/docs/reference/options/#titlebar) on macOS. To display the form correctly with `mac.TitleBarHiddenInset()` enabled, set the `inset` parameter to layout. In this mode, the header is displayed on the app side (`title` propeprty of `inset` object), not the window side. Also the css parameter `--wails-draggable` is set in place of the header to make the window move correctly.

<Story of={Stories.Inset} />

## Scroll

PlainLayout can handle long content and supports scrolling.

<Story of={Stories.Scroll} />

## Transparent Titlebar

The Toolbar of an application can be transparent. This is used in macOS interfaces.

<Story of={Stories.TransparentTitlebar} />

## Titlebar

Layout can contain a Toolbar. The actions available in the application can be placed there. To do this, pass the element to the `toolbar` slot

<Story of={Stories.Titlebar} />

## Custom Toolbar

<Story of={Stories.CustomToolbar} />
