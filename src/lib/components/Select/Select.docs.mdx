import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { Source } from '@storybook/blocks'
import Select from './Select.svelte'
import * as Stories from './Select.stories.svelte'
import { Intro, Props } from '../../../../.storybook/components'

<Meta of={Stories} />

<Intro
  name="Select"
  description="Component with which the user selects a value from a set of suggested options."
/>

<Canvas>
  <Story of={Stories.Basic} />
</Canvas>

## Properties

<Props />

## Usage recommendations

- Use Select when users are selecting only a single item from a set;
- And set is too long to show all available options at once.

## Form

Select automatically changes the style if it is inside a [FormGroup](/?path=/docs/components-formgroup--docs) component.

<Canvas>
  <Story of={Stories.Form} />
</Canvas>

## Disabled

To disable interaction with a component use the `disabled` property.

<Canvas>
  <Story of={Stories.Disabled} />
</Canvas>
